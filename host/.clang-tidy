---
# Clang-tidy configuration for the host test/runner harness.
#
# The check set mirrors the root .clang-tidy (this nearest config fully
# replaces it for files under host/); the only difference is the host-only
# check exclusions below.
#
# Check exclusions mirror the root .clang-tidy (see its comment block),
# plus host-only exclusions:
# - bugprone-unchecked-string-to-number-conversion: the replay runners
#   parse machine-generated replay files; every sscanf is count-checked
#   (and the configuration line additionally zero-checked), and converting
#   to strtoul/strtol across the four runners would be scaffolding churn
#   for a tool that parses its own output.
# - clang-analyzer-optin.*: see the root .clang-tidy (opt-in analyzers are
#   enabled explicitly, not via the clang-analyzer-* wildcard).
Checks: >
  bugprone-*,
  -bugprone-easily-swappable-parameters,
  -bugprone-unchecked-string-to-number-conversion,
  clang-analyzer-*,
  -clang-analyzer-security.insecureAPI.*,
  -clang-analyzer-optin.*,
  misc-*,
  -misc-include-cleaner,
  -misc-non-private-member-variables-in-classes,
  performance-*,
  -performance-enum-size,
  portability-*,
  -portability-avoid-pragma-once
WarningsAsErrors: '*'
