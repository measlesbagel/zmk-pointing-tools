# Web tuner

The tuner will be an offline-capable static application served from localhost
or GitHub Pages. It will communicate directly with the keyboard using Web
Serial and will not require a backend, analytics, remote fonts, or secrets.
